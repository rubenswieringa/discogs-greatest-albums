import styled, { CSSObject } from 'styled-components';

export const HeaderH1 = styled.h1({});

export const HeaderSubheader = styled.p({});

const layout: CSSObject = {};

export const HeaderLayout = styled.header(layout);
